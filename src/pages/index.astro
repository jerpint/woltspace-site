---
// The landing page is hand-crafted HTML (src/landing.html), served verbatim.
// Edit src/landing.html directly — this route just passes it through.
import html from '../landing.html?raw';
---
<Fragment set:html={html} />
